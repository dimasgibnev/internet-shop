import mongoose from "mongoose";

const lineSchema = new mongoose.Schema(
  {
    title: {
      type: String,
      required: true,
      unique: true,
      index: true,
    },
  },
  {
    timestamps: true,
  }
);

export const LineModel = mongoose.model("Line", lineSchema);
